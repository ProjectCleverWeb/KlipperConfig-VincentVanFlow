[gcode_macro EXTRUDE]
description: Extrude a specific amount of filament
gcode:
	_MACRO_START NAME="EXTRUDE"
	{% set LENGTH = params.LENGTH|default(50)|float %}
	{% set SPEED = params.SPEED|default(180)|float %}
	G91 ; Relative positioning
	G1 E{LENGTH} F{SPEED}; Extrude
	_MACRO_END NAME="EXTRUDE"